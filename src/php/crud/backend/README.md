# PHP API

The API is intentionally framework-free and uses PDO.

Run locally:

```bash
php -S localhost:8080
```

Check:

```text
http://localhost:8080/api.php?action=schema
```

Requirements:
- PHP 8.2+
- PDO
- pdo_mysql
